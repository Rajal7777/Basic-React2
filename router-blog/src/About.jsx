import React from 'react';
import './index.css';

const About = () => {
  return (
    <main className="About">
      <h2>About</h2>
      <p>This is a simple blog application built with React and React Router. It allows users to create, view, and delete blog posts. The application demonstrates the use of React components, state management with hooks, and client-side routing.</p>
    </main>
  );
};

export default About;